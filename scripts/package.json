{
  "name": "@cadence/scripts",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "sign-mandate": "tsx src/sign-mandate.ts",
    "sign-mandate:testnet": "CASPER_NETWORK=testnet tsx src/sign-mandate.ts",
    "sign-mandate:mainnet": "CASPER_NETWORK=mainnet tsx src/sign-mandate.ts",
    "deploy-adapter": "tsx src/deploy-adapter.ts",
    "deploy-adapter:testnet": "CASPER_NETWORK=testnet tsx src/deploy-adapter.ts",
    "deploy": "tsx src/deploy.ts",
    "deploy:testnet": "CASPER_NETWORK=testnet tsx src/deploy.ts",
    "deploy:mainnet": "CASPER_NETWORK=mainnet tsx src/deploy.ts",
    "slice": "tsx src/slice.ts",
    "slice:testnet": "CASPER_NETWORK=testnet tsx src/slice.ts",
    "enable-and-slice": "tsx src/enable-and-slice.ts",
    "enable-and-slice:testnet": "CASPER_NETWORK=testnet tsx src/enable-and-slice.ts",
    "fund": "tsx src/fund.ts",
    "fund:testnet": "CASPER_NETWORK=testnet tsx src/fund.ts",
    "fund:mainnet": "CASPER_NETWORK=mainnet tsx src/fund.ts",
    "register-vault": "tsx src/register-vault.ts",
    "register-vault:testnet": "CASPER_NETWORK=testnet tsx src/register-vault.ts",
    "register-vault:mainnet": "CASPER_NETWORK=mainnet tsx src/register-vault.ts",
    "demo": "tsx src/demo.ts",
    "demo:testnet": "CASPER_NETWORK=testnet tsx src/demo.ts",
    "demo:mainnet": "CASPER_NETWORK=mainnet tsx src/demo.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@cadence/agent": "0.1.0",
    "@cadence/mandate": "0.1.0",
    "casper-js-sdk": "5.0.12",
    "dotenv": "17.4.2",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "22.10.5",
    "tsx": "4.19.2",
    "typescript": "5.7.3"
  }
}
