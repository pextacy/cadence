{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm install && npm run build --workspace @cadence/mandate && npm run build --workspace @cadence/dashboard",
  "outputDirectory": "dashboard/dist",
  "framework": null
}
